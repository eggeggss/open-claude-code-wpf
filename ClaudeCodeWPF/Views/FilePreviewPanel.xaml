<UserControl x:Class="OpenClaudeCodeWPF.Views.FilePreviewPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1E1E1E">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#252526" BorderBrush="#3C3C3C" BorderThickness="0,0,0,1" Padding="12,6">
            <TextBlock x:Name="FilePathText" Text="檔案預覽"
                       Foreground="#CCCCCC" FontSize="12" FontWeight="SemiBold"
                       TextTrimming="CharacterEllipsis"/>
        </Border>

        <!-- File content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <TextBox x:Name="FileContent"
                     Background="#1E1E1E" Foreground="#D4D4D4"
                     BorderThickness="0" Padding="12"
                     FontFamily="Consolas" FontSize="12"
                     IsReadOnly="True"
                     TextWrapping="NoWrap"
                     AcceptsReturn="True"/>
        </ScrollViewer>
    </Grid>
</UserControl>
